modulname = "0100 - Standard (TW GridBuilder)"
modulkey  = "tw_gridbuilder"
version   = "2.9.0"
requirements = "tw_gridbuilder"
status    = 1; 0 = geplant, 1 = fertig, 2 = in Bearbeitung
framework = 1; 0 = egal, 1 = Tailwind, 2 = Bootstrap

; Diese Konfiguration liegt absichtlich im TW GridBuilder-Addon, damit das
; Base-Addon sie als Quelle nutzen kann und Modul-Metadaten zentral bleiben.
; Die Version wird durch das Base-Addon aus dieser Datei gelesen.
